#include <stdio.h>
#include <stdlib.h>

int main()
{
int n;
printf("Enter an integer: ");
scanf("%d",&n);
if(n<0)
{
printf("The entered integer is negative:");
}
return 0;
}
C Program to dispnumlay a ber if it is negative.c
